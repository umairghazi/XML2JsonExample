How to run this script. 

1. Start the terminal window in the current directory.
2. Make sure you have Node.js and node package manager (NPM) installed.
3. Run the command - npm start
4. The application will look for cookbook.xml in the current directory and convert it to cookbook.json. It will also print the json on console (terminal window running the server).
